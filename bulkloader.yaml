# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.api.datastore
- import: google.appengine.api.users
- import: google.appengine.ext.db
- import: impexp

transformers:

- kind: AuthToken
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: created
      external_name: created
      # Type: Date/Time Stats: 156 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: id
      external_name: id
      # Type: String Stats: 8 properties of this type in this kind.

    - property: location
      external_name: location
      # Type: String Stats: 13 properties of this type in this kind.

    - property: name
      external_name: name
      # Type: String Stats: 57 properties of this type in this kind.

    - property: secret
      external_name: secret
      # Type: String Stats: 111 properties of this type in this kind.

    - property: service
      external_name: service
      # Type: String Stats: 156 properties of this type in this kind.

    - property: token
      external_name: token
      # Type: String Stats: 156 properties of this type in this kind.

    - property: url
      external_name: url
      # Type: Link Stats: 53 properties of this type in this kind.
      import_transform: transform.none_if_empty(db.Link)


- kind: Post
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: confirmed_at
      external_name: confirmed_at
      # Type: Date/Time Stats: 20 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: created_at
      external_name: created_at
      # Type: Date/Time Stats: 23 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: description
      external_name: description
      # Type: String Stats: 23 properties of this type in this kind.

    - property: email
      external_name: email
      # Type: Email Stats: 23 properties of this type in this kind.
      import_transform: db.Email

    - property: found
      external_name: found
      # Type: Boolean Stats: 2 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: found_at
      external_name: found_at
      # Type: Date/Time Stats: 1 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: location
      external_name: location
      # Type: GeoPt Stats: 23 properties of this type in this kind.

    - property: photo
      external_name: photo
      # Type: Blob Stats: 23 properties of this type in this kind.
      export_transform: base64.b64encode
      import_transform: impexp.blobproperty_from_base64_with_img_resize(600)

    - property: secret
      external_name: secret
      # Type: String Stats: 23 properties of this type in this kind.

    - property: service_updated_at
      external_name: service_updated_at
      # Type: Date/Time Stats: 18 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: stolen_at
      external_name: stolen_at
      # Type: Date/Time Stats: 19 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: stolen_on
      external_name: stolen_on
      # Type: Date/Time Stats: 23 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: thumbnail
      external_name: thumbnail
      # Type: Blob Stats: 23 properties of this type in this kind.
      export_transform: base64.b64encode
      import_transform: impexp.blobproperty_from_base64_with_img_resize(250)

    - property: thumbnail_height
      external_name: thumbnail_height
      # Type: Integer Stats: 23 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: updated_at
      external_name: updated_at
      # Type: Date/Time Stats: 23 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: user
      external_name: user
      # Type: Key Stats: 20 properties of this type in this kind.
      import_transform: transform.create_foreign_key('AuthToken')
      export_transform: transform.key_id_or_name_as_string

